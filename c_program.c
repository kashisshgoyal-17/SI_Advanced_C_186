#include <stdio.h>
#include<conio.h>
int main()
{
    int rows,totalrows;
    printf("enter the no of rows");
    scanf("%d",&rows);
    totalrows=2*rows;
    for(i=1;i<total rows;i++)
    {
        for(j=1;j<total rows;j++)
        {
            prinf(" ");
        }
        for(j=1;j<2*i-1;j++)
        {
            printf("*");
        }
        printf("\n");
    }
    return 0;
}
